-- identifying fds; look at single attributes, then groups
-- type of relationship implied by fds, e.g. A->B, B->A 1:1 

-- if attribute-closure gives all attributes, then candidate key

-- determining candidate key; will see attribute that only ever appear on LHS or not in anything at all
